from core.format import CollectFormat


class CMPCollectFormat(CollectFormat):
    code = "cmp_collect_base"
    type = "cmp_collect"
    tag = "cmp.collect"
    name = "驱动数据转换(cmp)"
    desc = "驱动数据转换(cmp)"
    format_map = {}
